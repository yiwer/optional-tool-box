# P2 Backlog 清障实施计划（compare / storage / llm 终审延后项）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 清空五模块 P1 收官时累积的 12 项终审延后 Minor（compare 4 项语义缺口 + 1 项加固、storage 3 项约定补齐、llm 4 项接线/测试/文档），全部为修补型改动，不引入新特性面。

**Architecture:** 按模块分组的 12 个独立小任务 + 1 个全仓门禁任务，全部在单一分支 `feat/p2-backlog` 上按序落地；每任务独立可测试、独立提交，互相无接口依赖（唯一例外：Task 9 与 Task 10 都改 llm 两个 record 的 `toString`，须按序执行）。compare 改动集中在 `engine`/`core` 包内部，不触碰 SPI 与装配契约；storage 的 tika retrofit 照抄 mail 的实例级 probeClassLoader 既有范式；llm 的 response_format 接线消费掉唯一死常量。

**Tech Stack:** Java 21、Spring Boot 3.5.10（BOM 管版本）、JUnit 5 + AssertJ、WireMock（llm 契约测试）、ApplicationContextRunner + FilteredClassLoader（装配测试）、Maven（无 mvnw，直接 `mvn`）。

## Global Constraints

（摘自 docs/design/00-overview.md 全局约束与既有里程碑裁定，每个任务隐含遵守）

- 不引入任何新第三方运行时依赖；不用 Lombok。
- 对外 API 一律返回 `cn.code91.facility.result.Result<T,E>`，公开 API 零异常外抛（database 豁免与本计划无关）。
- jacoco 门禁：INSTRUCTION ≥ 0.80 / LINE ≥ 0.80 / BRANCH ≥ 0.70（verify 阶段检查）；`maven-dependency-plugin` analyze failOnWarning。新增分支必须带测试。
- i18n：bundle 路径 `i18n/toolbox-{module}-messages*.properties`，模块自有键必须带 `toolbox.{module}.` 前缀（防 facility `AggregatedMessageSource` 聚合池污染，compare I4 教训）。
- 每模块三条 ArchUnit 规则不得破坏（顶层切片无环 / `autoconfigure` 不被其它包依赖 / 门面包不依赖 autoconfigure 或 SDK/HTTP 类型）。本计划所有改动都在既有包内部，不新增跨包依赖边。
- 测试风格：JUnit 5 + AssertJ（`assertThat`/`assertThatThrownBy`），引擎与守卫测试用真实夹具不用 Mockito，装配测试用 `ApplicationContextRunner`，断言带中文 `.as(...)` 描述。
- 提交信息格式：`{module}: 中文摘要`（对齐 git log 既有风格），**小步频繁提交**——过程教训：子代理中断时小步提交是唯一保险。
- 分支 `feat/p2-backlog`（Task 1 步骤 0 创建）；合并回 master 须先过 Task 13 全仓 `mvn clean verify`（含 PG/MinIO Docker IT）。
- Shell 为 Windows PowerShell：命令按 PowerShell 语法书写（不用 `&&`）。

## 范围裁定（本计划不含）

以下是 **feature 级 roadmap P2 项**，不是终审延后 Minor，需各自先走本项目惯例的"设计文档 + 控制器裁定"轮，不塞进本修补计划：

- **storage 分片上传全家桶**（multipart initiate/uploadPart/complete/abort，OSS/S3/local 三端 API 形态未决）；
- **compare 集合三策略完整实现**（`@CompareKey`/BY_KEY/AS_SET，M0-M1 裁定 B 收窄）与 `Hashing` 快速预判等；
- **llm SSE 流式 + chatAsync**（M4 裁定 A 剔除出 P1，本计划 Task 12 只补文档 supersede 注）、Spring AI 桥接（P3）。

另：storage 存量硬编码中文错误消息迁移进 i18n bundle 属后续独立决策，Task 8 只补 MessageSource 基础设施（与 compare 现状对齐——compare 生产 bundle 同样无真实键）。

## 文件结构总览

| 任务 | 改动文件（Create/Modify） |
|---|---|
| T1 | M `compare/engine/LeafValues.java`、M `ReflectionDiffEngineLeafTest.java`、M `ReflectionDiffEngineErrorTest.java`、M 夹具 `DateFieldBean/UuidFieldBean` Javadoc、M `docs/design/01-compare-enhancement.md` §4.3 |
| T2 | M `LeafValues.java`、M `compare/engine/ReflectionDiffEngine.java`（isComparableTypePair）、C 夹具 `PaymentStage.java`、M `ReflectionDiffEngineLeafTest.java` |
| T3 | M `compare/core/DiffOptions.java`、M `DiffOptionsTest.java`、M `ReflectionDiffEnginePathFilterTest.java` |
| T4 | M `ReflectionDiffEngine.java`（compare 入口归一）、M `ReflectionDiffEngineErrorTest.java` |
| T5 | M `compare/engine/LabelResolver.java`、M `LabelLocaleRegressionTest.java` |
| T6 | M `storage/core/ObjectStore.java`、M `storage/local/LocalFsObjectStore.java`（均仅 Javadoc） |
| T7 | M `storage/guard/DefaultStorageGuard.java`、M `storage/autoconfigure/ToolboxStorageAutoConfiguration.java`、M `DefaultStorageGuardTest.java` |
| T8 | C `storage-enhancement/src/main/resources/i18n/toolbox-storage-messages.properties`（+`_zh_CN`）、M `ToolboxStorageAutoConfiguration.java`、M `ToolboxStorageAutoConfigurationTest.java` |
| T9 | M `llm/autoconfigure/ToolboxLlmProperties.java`（仅 toString）、M `ToolboxLlmPropertiesTest.java` |
| T10 | M `ToolboxLlmProperties.java`、M `llm/openai/OpenAiModelConfig.java`、M `llm/autoconfigure/ToolboxLlmAutoConfiguration.java`、M `llm/openai/OpenAiCompatibleClient.java`、M 多个 llm 测试（构造点追参）、M `OpenAiCompatibleClientStructuredOutputTest.java`、M `docs/design/03-llm-enhancement.md` §6 |
| T11 | M `llm/openai/OpenAiCompatibleClientLoggingTest.java`（仅注解） |
| T12 | M `docs/design/03-llm-enhancement.md`、M `docs/design/00-overview.md`（均仅文档） |
| T13 | 无文件改动（全仓门禁） |

（compare 路径省略前缀 `compare-enhancement/src/{main,test}/java/cn/code91/toolbox/`，storage/llm 同理。）

---

### Task 1: compare — 叶子类型表扩 Date/UUID

`java.util.Date` 与 `UUID` 目前不在叶子表内，走对象图递归 → 命中 JDK 模块反射限制，只能返回 `FieldAccessError`（M1 I2 修复把异常收敛成 Err，扩表明确留 P2，见 `.superpowers/sdd/task-4-fixes-report.md:78`）。本任务把两者纳入叶子：直接判等 + 文本渲染，原"返回 Err"回归测试语义反转。

**Files:**
- Modify: `compare-enhancement/src/main/java/cn/code91/toolbox/compare/engine/LeafValues.java`
- Modify: `compare-enhancement/src/test/java/cn/code91/toolbox/compare/engine/ReflectionDiffEngineLeafTest.java`
- Modify: `compare-enhancement/src/test/java/cn/code91/toolbox/compare/engine/ReflectionDiffEngineErrorTest.java`
- Modify: `compare-enhancement/src/test/java/cn/code91/toolbox/compare/testfixtures/DateFieldBean.java`（仅 Javadoc）
- Modify: `compare-enhancement/src/test/java/cn/code91/toolbox/compare/testfixtures/UuidFieldBean.java`（仅 Javadoc）
- Modify: `docs/design/01-compare-enhancement.md:86`（§4.3 叶子类型清单）

**Interfaces:**
- Consumes: 无（首任务）。
- Produces: `LeafValues.isLeaf(Class<?>)` 对 `Date` 子类与 `UUID` 返回 true；`LeafValues.toText(Object, String)` 对 `Date` 走 `DateUtil` 按 `datePattern` 渲染。后续任务不依赖本任务改动。

- [ ] **Step 0: 创建分支**

```powershell
git checkout master; git checkout -b feat/p2-backlog
```

- [ ] **Step 1: 写失败测试（正向叶子行为）**

在 `ReflectionDiffEngineLeafTest.java` 追加（import 区补 `cn.code91.toolbox.compare.testfixtures.DateFieldBean`、`cn.code91.toolbox.compare.testfixtures.UuidFieldBean`、`java.util.Date`、`java.util.UUID`）：

```java
    @Test
    void dateFieldComparesAsLeaf() {
        DateFieldBean before = new DateFieldBean();
        before.setCreatedAt(new Date(1000L));
        DateFieldBean after = new DateFieldBean();
        after.setCreatedAt(new Date(2000L));

        Result<DiffResult, CompareError> result = engine.diff(before, after);

        assertThat(result.isOk()).as("P2 扩表后 Date 是叶子类型，不再走对象图反射").isTrue();
        assertThat(result.get().changes()).hasSize(1);
        var change = result.get().changes().get(0);
        assertThat(change.path()).isEqualTo("createdAt");
        assertThat(change.kind()).isEqualTo(ChangeKind.MODIFIED);
        assertThat(change.oldText())
                .as("Date 展示文本应经 DateUtil 按 datePattern 渲染（yyyy-MM-dd HH:mm:ss）")
                .matches("\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}");
    }

    @Test
    void equalDatesProduceNoChanges() {
        DateFieldBean before = new DateFieldBean();
        before.setCreatedAt(new Date(1000L));
        DateFieldBean after = new DateFieldBean();
        after.setCreatedAt(new Date(1000L));

        Result<DiffResult, CompareError> result = engine.diff(before, after);

        assertThat(result.isOk()).isTrue();
        assertThat(result.get().identical()).isTrue();
    }

    @Test
    void uuidFieldComparesAsLeaf() {
        UuidFieldBean before = new UuidFieldBean();
        before.setId(UUID.fromString("11111111-1111-1111-1111-111111111111"));
        UuidFieldBean after = new UuidFieldBean();
        after.setId(UUID.fromString("22222222-2222-2222-2222-222222222222"));

        Result<DiffResult, CompareError> result = engine.diff(before, after);

        assertThat(result.isOk()).as("P2 扩表后 UUID 是叶子类型").isTrue();
        assertThat(result.get().changes()).hasSize(1);
        var change = result.get().changes().get(0);
        assertThat(change.path()).isEqualTo("id");
        assertThat(change.oldText()).isEqualTo("11111111-1111-1111-1111-111111111111");
        assertThat(change.newText()).isEqualTo("22222222-2222-2222-2222-222222222222");
    }
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
mvn -pl compare-enhancement test -Dtest=ReflectionDiffEngineLeafTest
```

预期：新增 3 例 FAIL（当前 `diff` 对 Date/UUID 返回 Err，`result.isOk()` 为 false）。

- [ ] **Step 3: 实现——LeafValues 扩表**

`LeafValues.java` 全文替换为：

```java
package cn.code91.toolbox.compare.engine;

import cn.code91.facility.date.DateUtil;

import java.math.BigDecimal;
import java.time.Instant;
import java.time.ZoneId;
import java.time.temporal.TemporalAccessor;
import java.util.Date;
import java.util.UUID;

/**
 * 叶子类型判定与展示文本渲染。叶子类型直接比较，不再递归展开字段。
 */
final class LeafValues {

    private LeafValues() {
    }

    /**
     * 是否为叶子类型：基本类型/包装、String、枚举、日期时间（{@code TemporalAccessor} 与
     * {@code java.util.Date} 含其 {@code java.sql} 子类，P2 扩表）、{@code UUID}（P2 扩表）、
     * BigDecimal。数值类型统一视为叶子（不含 BigDecimal 以外的 Number 特殊判等逻辑，走 equals）。
     */
    static boolean isLeaf(Class<?> type) {
        return type.isPrimitive()
                || type.isEnum()
                || CharSequence.class.isAssignableFrom(type)
                || Number.class.isAssignableFrom(type)
                || Boolean.class.isAssignableFrom(type)
                || Character.class.isAssignableFrom(type)
                || TemporalAccessor.class.isAssignableFrom(type)
                || Date.class.isAssignableFrom(type)
                || UUID.class == type;
    }

    /**
     * 渲染叶子值的展示文本。日期时间经 facility {@code DateUtil} 按 {@code datePattern} 渲染
     * （{@code Date} 经 epoch 毫秒转 {@code LocalDateTime}——不用 {@code Date#toInstant()}，
     * {@code java.sql.Date}/{@code java.sql.Time} 对其抛 {@code UnsupportedOperationException}）；
     * 其余类型用 {@code toString()}（枚举即 {@code name()}）。
     */
    static String toText(Object value, String datePattern) {
        if (value == null) {
            return null;
        }
        if (value instanceof TemporalAccessor temporal) {
            return DateUtil.format(temporal, datePattern).orElse(value.toString());
        }
        if (value instanceof Date date) {
            return DateUtil.format(
                            Instant.ofEpochMilli(date.getTime()).atZone(ZoneId.systemDefault()).toLocalDateTime(),
                            datePattern)
                    .orElse(value.toString());
        }
        return value.toString();
    }

    /**
     * 判定两个叶子值是否相等：{@link BigDecimal} 用 {@code compareTo} 规避 scale 陷阱（1.0 与 1.00 相等）；
     * 其余类型用 {@code equals}（{@code Date}/{@code UUID} 的 equals 均为值语义；{@code Timestamp}
     * 对 {@code Date} 的非对称 equals 不可达——类型对检查要求两侧运行时类相同）。
     */
    static boolean equalsLeaf(Object a, Object b) {
        if (a instanceof BigDecimal bd1 && b instanceof BigDecimal bd2) {
            return bd1.compareTo(bd2) == 0;
        }
        return java.util.Objects.equals(a, b);
    }
}
```

- [ ] **Step 4: 反转 I2 回归测试语义**

`ReflectionDiffEngineErrorTest.java`：**删除** `dateFieldReturnsErrInsteadOfThrowing`（原 :196-212，含其 Javadoc）与 `uuidFieldReturnsErrInsteadOfThrowing`（原 :214-228，含其 Javadoc）两个方法——正向行为已由 Step 1 的 LeafTest 用例钉住；同时删除因此不再使用的 import：`cn.code91.toolbox.compare.testfixtures.DateFieldBean`、`cn.code91.toolbox.compare.testfixtures.UuidFieldBean`、`java.util.Date`、`java.util.UUID`。**保留** `setFieldReturnsErrInsteadOfThrowing`（Set 仍按裁定 B 拒绝）。

- [ ] **Step 5: 更新夹具 Javadoc（事实已变，注释不得说谎）**

`DateFieldBean.java` 类 Javadoc 替换为：

```java
/**
 * 含 {@code java.util.Date} 字段的测试夹具。P2 扩表后 {@code Date} 已是叶子类型
 * （{@code LeafValues#isLeaf}），供叶子矩阵正向用例使用；历史上曾是 I2 回归夹具
 * （扩表前 Date 走对象图反射并收敛为 Err）。
 */
```

`UuidFieldBean.java` 类 Javadoc 替换为：

```java
/**
 * 含 {@code UUID} 字段的测试夹具。P2 扩表后 {@code UUID} 已是叶子类型
 * （{@code LeafValues#isLeaf}），供叶子矩阵正向用例使用；历史上曾是 I2 回归夹具。
 */
```

- [ ] **Step 6: 更新设计文档 §4.3**

`docs/design/01-compare-enhancement.md:86` 的叶子类型清单行，把

```
1. **叶子类型**直接比：基本类型/包装、String、枚举（渲染取 `@CompareLabel` 或 name）、日期时间（`DateUtil` 格式化）、`BigDecimal`（**`compareTo` 判等**，规避 scale 陷阱）。
```

替换为：

```
1. **叶子类型**直接比：基本类型/包装、String、枚举（渲染取 `@CompareLabel` 或 name）、日期时间（`TemporalAccessor` 与 `java.util.Date` 含 `java.sql` 子类，均 `DateUtil` 格式化；Date 为 P2 扩表）、`UUID`（P2 扩表）、`BigDecimal`（**`compareTo` 判等**，规避 scale 陷阱）。
```

- [ ] **Step 7: 跑模块测试确认全绿**

```powershell
mvn -pl compare-enhancement test
```

预期：PASS（含 Step 1 三例转绿、ErrorTest 删两例后其余全绿）。

- [ ] **Step 8: 提交**

```powershell
git add compare-enhancement docs/design/01-compare-enhancement.md
git commit -m "compare-enhancement: 叶子类型表扩 Date/UUID（P2），I2 回归用例语义反转"
```

---

### Task 2: compare — 带方法体枚举（匿名子类常量）

带常量体的枚举常量运行时类是合成匿名子类（`E$1`，`Class.isEnum()` 为 false）：(a) 逃出叶子表误入对象图；(b) 两个不同带体常量运行时类不同，被 `isComparableTypePair` 判为 `TypeMismatch` 直接 Err——"状态从 A 改成 B"这类最常见变更被误报为错误。判定与类型对检查都改用 `Enum` 语义。

**Files:**
- Modify: `compare-enhancement/src/main/java/cn/code91/toolbox/compare/engine/LeafValues.java`
- Modify: `compare-enhancement/src/main/java/cn/code91/toolbox/compare/engine/ReflectionDiffEngine.java:370-377`
- Create: `compare-enhancement/src/test/java/cn/code91/toolbox/compare/testfixtures/PaymentStage.java`
- Modify: `compare-enhancement/src/test/java/cn/code91/toolbox/compare/engine/ReflectionDiffEngineLeafTest.java`

**Interfaces:**
- Consumes: Task 1 后的 `LeafValues`（若 Task 1 未执行，isLeaf 行数不同但改动点相同）。
- Produces: `LeafValues.isLeaf` 对枚举匿名子类返回 true；`LeafValues.toText` 对 `Enum` 恒返回 `name()`；`ReflectionDiffEngine.isComparableTypePair` 对同声明枚举的不同常量返回 true。

- [ ] **Step 1: 创建夹具**

`PaymentStage.java`：

```java
package cn.code91.toolbox.compare.testfixtures;

/**
 * 带方法体（常量体）的枚举夹具（P2）：AUTHORIZED/CAPTURED 的运行时类是合成匿名子类
 * （{@code PaymentStage$1}/{@code PaymentStage$2}，{@code Class.isEnum()} 为 false），
 * 用于验证叶子判定与类型对可比性按 {@code Enum} 语义而非运行时 Class 处理。
 * {@code toString} 故意覆写为中文描述，验证展示文本取 {@code name()} 而非 {@code toString()}。
 */
public enum PaymentStage {
    AUTHORIZED {
        @Override
        public String toString() {
            return "已授权";
        }
    },
    CAPTURED {
        @Override
        public String toString() {
            return "已扣款";
        }
    }
}
```

- [ ] **Step 2: 写失败测试**

`ReflectionDiffEngineLeafTest.java` 追加（import 区补 `cn.code91.toolbox.compare.core.TypeMismatch`、`cn.code91.toolbox.compare.testfixtures.PaymentStage`）：

```java
    @Test
    void enumConstantsWithBodiesCompareAsLeafByName() {
        Result<DiffResult, CompareError> result = engine.diff(PaymentStage.AUTHORIZED, PaymentStage.CAPTURED);

        assertThat(result.isOk())
                .as("带方法体枚举常量（运行时类 PaymentStage$1/$2）不应被误判为 TypeMismatch")
                .isTrue();
        assertThat(result.get().changes()).hasSize(1);
        var change = result.get().changes().get(0);
        assertThat(change.kind()).isEqualTo(ChangeKind.MODIFIED);
        assertThat(change.oldText()).as("展示文本取 name() 而非被覆写的 toString()").isEqualTo("AUTHORIZED");
        assertThat(change.newText()).isEqualTo("CAPTURED");
    }

    @Test
    void sameEnumConstantWithBodyIsIdentical() {
        Result<DiffResult, CompareError> result = engine.diff(PaymentStage.AUTHORIZED, PaymentStage.AUTHORIZED);

        assertThat(result.isOk()).isTrue();
        assertThat(result.get().identical()).isTrue();
    }

    @Test
    void differentEnumTypesRemainTypeMismatch() {
        Result<DiffResult, CompareError> result = engine.diff((Object) OrderStatus.DRAFT, (Object) PaymentStage.AUTHORIZED);

        assertThat(result.isErr()).as("不同声明枚举之间仍是类型不一致").isTrue();
        assertThat(result.getErr()).isInstanceOf(TypeMismatch.class);
    }
```

- [ ] **Step 3: 跑测试确认失败**

```powershell
mvn -pl compare-enhancement test -Dtest=ReflectionDiffEngineLeafTest
```

预期：`enumConstantsWithBodiesCompareAsLeafByName` FAIL（isOk=false，当前返回 TypeMismatch Err）。

- [ ] **Step 4: 实现**

(a) `LeafValues.isLeaf` 中 `|| type.isEnum()` 替换为 `|| Enum.class.isAssignableFrom(type)`，并在该方法 Javadoc 的"枚举"处补一句：`（含带方法体常量的匿名子类——其 Class.isEnum() 为 false，故用 Enum 可赋值性判定）`。

(b) `LeafValues.toText` 在 `TemporalAccessor` 分支**之前**插入：

```java
        if (value instanceof Enum<?> enumValue) {
            // 带方法体常量可能覆写 toString；展示文本固定取 name()，保证稳定可读。
            return enumValue.name();
        }
```

(c) `ReflectionDiffEngine.java` 的 `isComparableTypePair`（:370-377）替换为：

```java
    private static boolean isComparableTypePair(Class<?> oldType, Class<?> newType) {
        if (oldType.equals(newType)) {
            return true;
        }
        // 带方法体的枚举常量运行时类是合成匿名子类（E$1/E$2），不同常量类不同；
        // 同一声明枚举下的常量互为可比较（P2：带方法体枚举）。
        if (Enum.class.isAssignableFrom(oldType) && Enum.class.isAssignableFrom(newType)) {
            return baseEnumClass(oldType).equals(baseEnumClass(newType));
        }
        // 同为 List 或同为 Map 的不同实现类（如 ArrayList vs LinkedList）视为可比较。
        return (List.class.isAssignableFrom(oldType) && List.class.isAssignableFrom(newType))
                || (Map.class.isAssignableFrom(oldType) && Map.class.isAssignableFrom(newType));
    }

    /**
     * 枚举常量的声明枚举类：普通常量运行时类即声明类（isEnum()=true）；带方法体常量的
     * 运行时类是匿名子类（isEnum()=false），其直接父类才是声明枚举类。
     */
    private static Class<?> baseEnumClass(Class<?> type) {
        return type.isEnum() ? type : type.getSuperclass();
    }
```

已知边界（不处理，随提交在 `isComparableTypePair` 注释中已隐含）：类型级 `ValueComparator`/`ValueFormatter` 注册表按运行时 Class 查找，对带体常量（`E$1`）查不到注册在 `E` 上的比较器——注册表查找语义本任务不动。

- [ ] **Step 5: 跑模块测试确认全绿**

```powershell
mvn -pl compare-enhancement test
```

预期：PASS（含既有 `enumChangeIsDetected`——普通枚举行为不变）。

- [ ] **Step 6: 提交**

```powershell
git add compare-enhancement
git commit -m "compare-enhancement: 带方法体枚举按 Enum 语义判叶子与类型对（P2）"
```

---

### Task 3: compare — include 深路径语义

`DiffOptions.isPathIncluded` 的 include 匹配只认"自身/后代"：include `"a.b"` 时顶层字段 `a` 在下钻前就被过滤，永远到不了 `a.b`，产出空 diff 且无测试暴露。修复：include 方向增加**祖先放行**（path 是某 include 前缀的祖先时放行以便下钻）；exclude 语义不变。

**Files:**
- Modify: `compare-enhancement/src/main/java/cn/code91/toolbox/compare/core/DiffOptions.java:33-49`
- Modify: `compare-enhancement/src/test/java/cn/code91/toolbox/compare/core/DiffOptionsTest.java`
- Modify: `compare-enhancement/src/test/java/cn/code91/toolbox/compare/engine/ReflectionDiffEnginePathFilterTest.java`

**Interfaces:**
- Consumes: 无。
- Produces: `DiffOptions.isPathIncluded(String)` 新语义——include 前缀的祖先路径返回 true。引擎侧 `compareField` 的调用点（`ReflectionDiffEngine.java:254`）无需改动。

- [ ] **Step 1: 写失败测试（单元层语义矩阵）**

`DiffOptionsTest.java` 追加：

```java
    @Test
    void deepIncludePathAllowsDescendingThroughAncestors() {
        DiffOptions options = new DiffOptions(8, false, Set.of("address.city"), Set.of());

        assertThat(options.isPathIncluded("address"))
                .as("祖先必须放行，否则引擎在顶层就跳过 address，永远下钻不到 address.city").isTrue();
        assertThat(options.isPathIncluded("address.city")).as("目标深路径本身").isTrue();
        assertThat(options.isPathIncluded("address.city.code")).as("目标之下的后代").isTrue();
        assertThat(options.isPathIncluded("address.street")).as("目标的兄弟不放行").isFalse();
        assertThat(options.isPathIncluded("amount")).as("无关顶层字段不放行").isFalse();
        assertThat(options.isPathIncluded("addressBook"))
                .as("字符串前缀相同但非路径分隔不得误判为祖先").isFalse();
    }

    @Test
    void deepIncludeWithIndexedPathAllowsAncestors() {
        DiffOptions options = new DiffOptions(8, false, Set.of("items[0].price"), Set.of());

        assertThat(options.isPathIncluded("items")).as("下标路径的顶层祖先").isTrue();
        assertThat(options.isPathIncluded("items[0]")).isTrue();
        assertThat(options.isPathIncluded("items[0].price")).isTrue();
        assertThat(options.isPathIncluded("items[1]")).as("其它下标不是 items[0].price 的祖先").isFalse();
    }

    @Test
    void excludeDoesNotGainAncestorSemantics() {
        DiffOptions options = new DiffOptions(8, false, Set.of(), Set.of("address.city"));

        assertThat(options.isPathIncluded("address")).as("exclude 深路径不得连带排除祖先").isTrue();
        assertThat(options.isPathIncluded("address.city")).isFalse();
        assertThat(options.isPathIncluded("address.street")).isTrue();
    }
```

`ReflectionDiffEnginePathFilterTest.java` 追加（引擎级端到端）：

```java
    @Test
    void deepIncludePathReachesNestedFieldThroughAncestors() {
        OrderBean before = new OrderBean();
        before.setAmount(new BigDecimal("100.00"));
        before.setAddress(new AddressBean("北京", "朝阳区"));
        OrderBean after = new OrderBean();
        after.setAmount(new BigDecimal("120.00"));
        after.setAddress(new AddressBean("上海", "海淀区"));

        DiffOptions options = new DiffOptions(8, false, Set.of("address.city"), Set.of());
        DiffResult result = engine.diff(before, after, options).get();

        assertThat(result.changes()).extracting("path")
                .as("include 深路径应下钻命中 address.city；amount 与 address 下的兄弟字段均不产出")
                .containsExactly("address.city");
    }
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
mvn -pl compare-enhancement test -Dtest=DiffOptionsTest+ReflectionDiffEnginePathFilterTest
```

预期：`deepIncludePathAllowsDescendingThroughAncestors`（`isPathIncluded("address")` 为 false）、`deepIncludeWithIndexedPathAllowsAncestors`、`deepIncludePathReachesNestedFieldThroughAncestors`（空 diff，containsExactly 失败）FAIL；`excludeDoesNotGainAncestorSemantics` 本就通过（钉住现状）。

- [ ] **Step 3: 实现**

`DiffOptions.java` 的 `isPathIncluded` 与 `matchesAny`（:33-49）替换为：

```java
    /**
     * 是否应比较给定路径（同时满足 include 与 exclude 约束）。
     *
     * <p>include 语义（P2 深路径修复）：path 命中某 include 前缀自身/其后代，<b>或是某个
     * include 前缀的祖先</b>时放行——祖先放行仅为允许引擎逐层下钻到目标深路径（include
     * {@code "a.b"} 时顶层字段 {@code a} 必须先放行才可能到达 {@code a.b}）。已知语义边界：
     * 若祖先字段本身是叶子值（或配有 {@code @CompareWith}/类型级比较器），它会被整体比较并
     * 可能产出自身的变更记录——include 深路径的本意是对象图下钻，对叶子祖先不适用。
     * exclude 无祖先语义：exclude {@code "a.b"} 只排除 {@code a.b} 及其后代，不影响
     * {@code a} 与 {@code a.c}。</p>
     */
    public boolean isPathIncluded(String path) {
        boolean includeOk = includePaths.isEmpty() || matchesAnyIncludePrefix(path);
        boolean excludeOk = excludePaths.isEmpty() || !matchesAny(path, excludePaths);
        return includeOk && excludeOk;
    }

    /** include 方向：自身/后代（同 exclude 的前缀语义），另放行 include 前缀的祖先。 */
    private boolean matchesAnyIncludePrefix(String path) {
        for (String prefix : includePaths) {
            if (matchesPrefix(path, prefix) || isAncestorOf(path, prefix)) {
                return true;
            }
        }
        return false;
    }

    private static boolean matchesAny(String path, Set<String> prefixes) {
        for (String prefix : prefixes) {
            if (matchesPrefix(path, prefix)) {
                return true;
            }
        }
        return false;
    }

    /** path 即 prefix 本身或其后代（{@code prefix.}/{@code prefix[} 分隔，避免 address/addressBook 误判）。 */
    private static boolean matchesPrefix(String path, String prefix) {
        return path.equals(prefix) || path.startsWith(prefix + ".") || path.startsWith(prefix + "[");
    }

    /** path 是 prefix 的祖先（prefix 位于 path 之下，如 path={@code a}、prefix={@code a.b} 或 {@code a[0]}）。 */
    private static boolean isAncestorOf(String path, String prefix) {
        return prefix.startsWith(path + ".") || prefix.startsWith(path + "[");
    }
```

同时把 record 头 Javadoc 的 `@param includePaths 仅比较这些路径（前缀匹配）；为空表示不限制` 改为 `@param includePaths 仅比较这些路径（前缀匹配，深路径的祖先自动放行以便下钻）；为空表示不限制`。

- [ ] **Step 4: 跑测试确认通过**

```powershell
mvn -pl compare-enhancement test
```

预期：PASS（含既有 `includePathsOnlyComparesMatchingFieldAndItsSubPaths`、`excludeTakesPrecedenceOverInclude` 等回归）。

- [ ] **Step 5: 提交**

```powershell
git add compare-enhancement
git commit -m "compare-enhancement: include 深路径祖先放行，修复深路径静默空 diff（P2）"
```

---

### Task 4: compare — nullAsEmpty 下沉到集合/Map 元素层

`normalizeNullAsEmpty` 目前只在 `compareField`（字段边界）套用；List/数组元素与 Map value 直接递归 `compare`，绕过归一——`nullAsEmpty=true` 时元素层 `null` vs `""` 仍报差异。修复：归一下沉到 `Traversal.compare()` 入口统一套用（字段层/元素层/顶层语义一致）。

**Files:**
- Modify: `compare-enhancement/src/main/java/cn/code91/toolbox/compare/engine/ReflectionDiffEngine.java`（compare 入口 :98、compareField :274、normalizeNullAsEmpty Javadoc :285-288）
- Modify: `compare-enhancement/src/test/java/cn/code91/toolbox/compare/engine/ReflectionDiffEngineErrorTest.java`

**Interfaces:**
- Consumes: 无。
- Produces: 行为变化——`nullAsEmpty=true` 时集合元素/Map value/顶层的 `""` 一律与 `null` 等价（此前仅字段层）。公开签名零变化。

- [ ] **Step 1: 写失败测试**

`ReflectionDiffEngineErrorTest.java` 追加（import 区补 `cn.code91.toolbox.compare.core.ChangeKind`、`java.util.Arrays`；`List`/`Map`/`HashMap`/`Set` 已有）：

```java
    @Test
    void nullAsEmptyTrueAppliesToListElements() {
        List<String> before = Arrays.asList("a", null);
        List<String> after = Arrays.asList("a", "");
        DiffOptions options = new DiffOptions(8, true, Set.of(), Set.of());

        DiffResult result = engine.diff(before, after, options).get();

        assertThat(result.identical())
                .as("nullAsEmpty=true 时集合元素层 null 与空串也应视为相等（P2 语义下沉）").isTrue();
    }

    @Test
    void nullAsEmptyTrueAppliesToMapValues() {
        Map<String, String> before = new HashMap<>();
        before.put("note", null);
        Map<String, String> after = new HashMap<>();
        after.put("note", "");
        DiffOptions options = new DiffOptions(8, true, Set.of(), Set.of());

        DiffResult result = engine.diff(before, after, options).get();

        assertThat(result.identical()).as("Map value 层同样生效").isTrue();
    }

    @Test
    void nullAsEmptyFalseStillReportsListElementDifference() {
        List<String> before = Arrays.asList("a", null);
        List<String> after = Arrays.asList("a", "");

        DiffResult result = engine.diff(before, after).get();

        assertThat(result.changes()).extracting("path").containsExactly("[1]");
        assertThat(result.changes().get(0).kind())
                .as("默认（nullAsEmpty=false）行为钉住：null→\"\" 记 ADDED").isEqualTo(ChangeKind.ADDED);
    }
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
mvn -pl compare-enhancement test -Dtest=ReflectionDiffEngineErrorTest
```

预期：前两例 FAIL（identical=false，元素层未归一）；第三例本就通过（钉现状）。

- [ ] **Step 3: 实现**

(a) `Traversal.compare`（:98）方法头与前三行改为在入口统一归一（形参改名 raw，方法体其余部分不动）：

```java
        void compare(String path, Supplier<String> labelSupplier, Object oldValueRaw, Object newValueRaw, int depth, List<FieldChange> changes) {
            Object oldValue = normalizeNullAsEmpty(oldValueRaw);
            Object newValue = normalizeNullAsEmpty(newValueRaw);
            if (oldValue == null && newValue == null) {
                return;
            }
```

并在该方法 Javadoc 末尾追加一行参数说明前的段落：

```java
         * <p>{@code nullAsEmpty} 归一在本方法入口统一套用（P2 下沉）：字段层、List/数组元素、
         * Map value、顶层实参语义一致；{@code @CompareWith} 字段分支不经过本方法，
         * 由 {@link #compareField} 单独归一。</p>
```

(b) `compareField` 常规分支（:274）去掉重复归一（入口已归一）：

```java
            compare(path, field::resolveLabel, oldFieldValue, newFieldValue, depth + 1, changes);
```

`@CompareWith` 分支（:260-272）**保持不动**（该分支不经过 `compare()`，归一仍在字段层完成）。

(c) `normalizeNullAsEmpty` Javadoc（:285-288）首句改为：

```java
        /**
         * {@code nullAsEmpty=true} 时把空串规整为 null，使 null 与 "" 在后续比较中视为相等。
         * 在 {@link #compare} 入口统一套用（P2：此前仅字段层生效，集合/Map 元素层与顶层被绕过）
         * （两者都归一后要么同为 null 直接判等，要么其中一侧仍为具体值走 ADDED/REMOVED）。
         */
```

- [ ] **Step 4: 跑模块测试确认全绿**

```powershell
mvn -pl compare-enhancement test
```

预期：PASS（含既有 `nullAsEmptyTrueTreatsNullAndBlankAsEqual` 字段层回归——双重归一改单点归一行为等价）。

- [ ] **Step 5: 提交**

```powershell
git add compare-enhancement
git commit -m "compare-enhancement: nullAsEmpty 归一下沉 compare 入口，集合/Map 元素层生效（P2）"
```

---

### Task 5: compare — labelSupplier 停机竞态加固

`diff()` 唯一异常收口是 `catch (CompareAbortException)`（`ReflectionDiffEngine.java:68`），而 `labelSupplier.get()` → `LabelResolver.resolve` → facility `LocaleUtil` → 进程级 `SpringContextHolder` → 容器 `MessageSource`。应用停机（context close）窗口内并发 diff，message 解析可抛 `IllegalStateException` 等运行时异常直接穿透 `diff()`，违反"对外 API 一律返回 Result"。标签是展示性信息——解析失败退回注解回退文本，不让整个 diff 失败。（终审 Minor 原文见 `.superpowers/sdd/progress.md:12`。）

**Files:**
- Modify: `compare-enhancement/src/main/java/cn/code91/toolbox/compare/engine/LabelResolver.java:29`
- Modify: `compare-enhancement/src/test/java/cn/code91/toolbox/compare/engine/LabelLocaleRegressionTest.java`

**Interfaces:**
- Consumes: 无。
- Produces: `LabelResolver.resolve(Field)` 保证不抛运行时异常（messageKey 解析失败退回 fallbackPattern）。

- [ ] **Step 1: 写失败测试**

`LabelLocaleRegressionTest.java` 改造：该类是全仓唯一触达 `SpringContextHolder` 的测试（其 Javadoc:33-38 已声明先到先得所有权），新用例必须**最后**执行并关闭它注入的上下文——需引入方法级顺序。

(a) import 区追加：

```java
import cn.code91.facility.result.Result;
import cn.code91.toolbox.compare.core.CompareError;
import org.junit.jupiter.api.MethodOrderer;
import org.junit.jupiter.api.Order;
import org.junit.jupiter.api.TestMethodOrder;
```

(b) 类声明加注解、静态块改留上下文引用：

```java
@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
class LabelLocaleRegressionTest {

    private static final ReflectionDiffEngine ENGINE = new ReflectionDiffEngine(CompareHandlerRegistry.withBuiltins());

    /** 静态注入的真实上下文；@Order 末位用例会将其 close 以复现停机竞态（见类 Javadoc 追注）。 */
    private static final AnnotationConfigApplicationContext CONTEXT;

    static {
        // 与生产 ToolboxCompareAutoConfiguration#toolboxCompareMessageSource 同构（除 basename
        // 指向测试专用 bundle 外）的真实 MessageSource，经由 SpringContextHolder 暴露给
        // LocaleUtil（其内部即经此单例查找）。
        CONTEXT = new AnnotationConfigApplicationContext(MessageSourceConfig.class);
        SpringContextHolder.setApplicationContextManually(CONTEXT);
    }
```

(c) 既有测试 `sameFieldLabelReflectsCurrentLocalePerDiffCallNotFirstCallerLocale` 方法上加 `@Order(1)`。

(d) 追加末位用例（关闭上下文后 label 解析必须回退而非穿透）：

```java
    /**
     * 停机竞态加固（M0/M1 终审 P2 候选）：SpringContextHolder 持有的上下文关闭后，
     * messageKey 解析不得让运行时异常穿透 diff()——应退回 @CompareLabel 注解回退文本。
     * 用英文 locale 断言：正常路径解析出 "Order Amount"，回退路径是注解 value "订单金额"，
     * 两者可区分（中文 locale 下解析结果与回退文本同为"订单金额"，无鉴别力）。
     * 本用例必须最后执行（@Order(2)）——close 不可逆，SpringContextHolder 无法 reset。
     */
    @Test
    @Order(2)
    void labelResolutionFallsBackInsteadOfThrowingWhenContextClosed() {
        CONTEXT.close();

        OrderBean before = new OrderBean();
        before.setAmount(new BigDecimal("100.00"));
        OrderBean after = new OrderBean();
        after.setAmount(new BigDecimal("200.00"));

        LocaleContextHolder.setLocale(Locale.ENGLISH);
        Result<DiffResult, CompareError> result = ENGINE.diff(before, after);

        assertThat(result.isOk())
                .as("MessageSource 上下文已关闭时 diff 不得异常穿透，应正常返回 Ok").isTrue();
        assertThat(labelOf(result.get(), "amount"))
                .as("messageKey 解析失败应退回 @CompareLabel 注解回退文本（而非英文解析结果）")
                .isEqualTo("订单金额");
    }
```

(e) 类 Javadoc 末段追加一句：`<p>另含停机竞态加固回归（@Order 末位）：关闭本类注入的上下文后验证标签解析回退——close 后其余 compare 测试不受影响（仅本类断言标签值，其余测试只断言 path/text）。`

- [ ] **Step 2: 跑测试确认失败**

```powershell
mvn -pl compare-enhancement test -Dtest=LabelLocaleRegressionTest
```

预期：新用例 FAIL——`diff` 抛出 `IllegalStateException`（closed context 的 getMessage 路径）穿透，或 `result` 非 Ok。
**契机分支**：若此步**意外通过**，说明 facility `LocaleUtil.translateMessageWithFallback` 已自行收敛运行时异常、泄漏点不存在——保留本用例作为回归钉，**跳过 Step 3**，提交信息注明"验证结论：facility 已收敛，仅补回归钉"。

- [ ] **Step 3: 实现**

`LabelResolver.java:29` 的返回语句替换为：

```java
        // fallbackPattern 走 MessageFormat 渲染；标签文本本身不含 {0} 占位符时原样返回。
        try {
            return LocaleUtil.translateMessageWithFallback(messageKey, null, fallbackPattern, LocaleUtil.getLocale());
        } catch (RuntimeException e) {
            // 停机竞态加固（M0/M1 终审 P2 候选）：diff() 唯一异常收口是 CompareAbortException，
            // 而 messageKey 解析经 facility SpringContextHolder 触达容器 MessageSource——应用
            // 停机（context close）窗口内并发 diff 可能抛 IllegalStateException 等运行时异常，
            // 任其穿透违反"对外 API 一律返回 Result"契约（00-overview §2 原则 3）。标签是
            // 展示性信息，解析失败退回注解回退文本即可，不值得让整个 diff 失败。
            return fallbackPattern;
        }
```

- [ ] **Step 4: 跑模块测试确认全绿**

```powershell
mvn -pl compare-enhancement test
```

预期：PASS。特别确认 `LabelLocaleRegressionTest` 两用例按序通过，且 `ToolboxCompareAutoConfigurationTest` 不受影响（它用自己的 ApplicationContextRunner bean，不经 SpringContextHolder）。

- [ ] **Step 5: 提交**

```powershell
git add compare-enhancement
git commit -m "compare-enhancement: labelSupplier 停机竞态加固——messageKey 解析异常退回注解文本（P2）"
```

---

### Task 6: storage — list 返回序 Javadoc 披露

`LocalFsObjectStore.list` 用 `Files.walk` 遍历，返回文件系统序（非字典序、非稳定序），方法零 Javadoc；接口 `ObjectStore.list` 也未约定顺序。终审 Minor：补披露。纯文档任务，无 TDD。

**Files:**
- Modify: `storage-enhancement/src/main/java/cn/code91/toolbox/storage/core/ObjectStore.java:37-40`
- Modify: `storage-enhancement/src/main/java/cn/code91/toolbox/storage/local/LocalFsObjectStore.java:95`（list 方法上方）

**Interfaces:** 无（Javadoc-only）。

- [ ] **Step 1: 接口 Javadoc**

`ObjectStore.java:37-40` 的 list Javadoc 替换为：

```java
    /**
     * 按前缀列出对象，语义为"key 以 prefix 开头"（非目录语义，无 prefix 内的分隔符收敛）；
     * {@code maxKeys} 限制单次返回条数，不做自动翻页。
     *
     * <p><b>返回顺序未定义</b>：由各 adapter 的底层遍历/服务端行为决定（云端多为字典序，
     * local 为文件系统遍历序），调用方不得依赖任何特定排序；需要有序结果请自行排序。
     * {@code maxKeys} 截断作用于该未定义顺序——不保证是字典序意义上的"前 N 个"。</p>
     */
```

- [ ] **Step 2: 实现类 Javadoc**

`LocalFsObjectStore.java` 的 `list` 方法（:95 `@Override` 上方）插入：

```java
    /**
     * {@inheritDoc}
     *
     * <p><b>返回序披露</b>（终审 Minor，P2 补注）：底层用 {@link Files#walk} 遍历，返回序即
     * 文件系统目录遍历序——<b>既非字典序也非跨平台稳定序</b>；{@code maxKeys} 截断的是该
     * 遍历序下的前 N 条。需要字典序请在调用方对结果排序（既有单测
     * {@code listReturnsOnlyKeysWithMatchingPrefix} 以 containsExactlyInAnyOrder 断言，
     * 即为此无序契约）。</p>
     */
```

- [ ] **Step 3: 编译验证 + 提交**

```powershell
mvn -pl storage-enhancement test -Dtest=LocalFsObjectStoreTest
```

预期：PASS（Javadoc 不影响行为）。

```powershell
git add storage-enhancement
git commit -m "storage-enhancement: list 返回序 Javadoc 披露——local 为文件系统序非字典序（P2）"
```

---

### Task 7: storage — tika 探测 retrofit 实例级 probeClassLoader

M2 终审建议（P2）：把 `DefaultStorageGuard` 的静态探测三件套（`static volatile` 缓存 + 硬编码 `DefaultStorageGuard.class.getClassLoader()` + 包私有静态 override/reset 测试钩子）retrofit 成 mail `DefaultAttachmentGuard` 的实例级范式（构造注入 `probeClassLoader` + 实例级 volatile 缓存），测试改用 `FilteredClassLoader` 真实演练缺 tika 分支，删除静态钩子与 `@AfterEach` 复位纪律。参照实现：`mail-enhancement/src/main/java/cn/code91/toolbox/mail/guard/DefaultAttachmentGuard.java:34-133` 与 `ToolboxMailAutoConfiguration.java:56-65`。

**Files:**
- Modify: `storage-enhancement/src/main/java/cn/code91/toolbox/storage/guard/DefaultStorageGuard.java`
- Modify: `storage-enhancement/src/main/java/cn/code91/toolbox/storage/autoconfigure/ToolboxStorageAutoConfiguration.java:49-55`
- Modify: `storage-enhancement/src/test/java/cn/code91/toolbox/storage/guard/DefaultStorageGuardTest.java`

**Interfaces:**
- Consumes: 无。
- Produces: 新公开构造器 `DefaultStorageGuard(GuardConfig config, ClassLoader probeClassLoader)`；既有单参构造器保留（回退本类加载器）。静态钩子 `overrideTikaPresent`/`resetTikaPresentCache` **删除**（包私有，无外部消费者）。

- [ ] **Step 1: 写失败测试（FilteredClassLoader 范式）**

`DefaultStorageGuardTest.java`：

(a) import 区追加 `org.springframework.boot.test.context.FilteredClassLoader;`（spring-boot-test 已在 storage 测试 classpath——`ToolboxStorageAutoConfigurationTest.java:13` 已用）。

(b) 把既有 `verifyMimeTrueRejectsWithGuidanceWhenTikaAbsentFromClasspath`（:244-258）替换为：

```java
    @Test
    void verifyMimeTrueRejectsWithGuidanceWhenTikaAbsentFromClasspath() {
        // FilteredClassLoader 屏蔽 org.apache.tika 包：探测走真实 Class.forName 失败分支
        // （P2 retrofit：不再用静态 override 钩子，同 mail DefaultAttachmentGuardTest 范式；
        //  00-overview.md §2 原则 7：verify-mime=true 而 Tika 缺失时拒绝并给引导，不静默降级）。
        GuardConfig config = new GuardConfig(1024 * 1024, Set.of(), true);
        DefaultStorageGuard guard = new DefaultStorageGuard(config, new FilteredClassLoader("org.apache.tika"));
        UploadCandidate candidate = candidateWith("photo.jpg", "image/jpeg", 10);

        Result<Void, StorageError> result = guard.check(candidate);

        assertThat(result.isErr()).isTrue();
        assertThat(result.getErr()).isInstanceOf(ValidationError.class);
        assertThat(result.getErr().message()).contains("tika-core");
    }

    @Test
    void verifyMimeFalseSucceedsEvenWhenTikaAbsent() {
        // verify-mime=false 时完全不探测：缺 tika 也放行（同 mail verifyMimeFalseNeverProbesTika 范式）。
        GuardConfig config = new GuardConfig(1024 * 1024, Set.of(), false);
        DefaultStorageGuard guard = new DefaultStorageGuard(config, new FilteredClassLoader("org.apache.tika"));
        UploadCandidate candidate = candidateWith("notes.txt", "text/plain", 10);

        Result<Void, StorageError> result = guard.check(candidate);

        assertThat(result.isOk()).isTrue();
    }
```

(c) 删除 `@AfterEach resetTikaProbeCache()`（:31-35）及 `org.junit.jupiter.api.AfterEach` import（实例级缓存随实例回收，无进程级污染，无需复位纪律）。

- [ ] **Step 2: 跑测试确认失败**

```powershell
mvn -pl storage-enhancement test -Dtest=DefaultStorageGuardTest
```

预期：**编译失败**（`DefaultStorageGuard` 无双参构造器、`resetTikaPresentCache` 引用已删）——即本任务的 RED 态。

- [ ] **Step 3: 实现——守卫 retrofit**

`DefaultStorageGuard.java`：

(a) 字段与构造器区（:39-57）替换为：

```java
    private static final String TIKA_PROBE_CLASS = "org.apache.tika.Tika";

    /**
     * MIME 魔数嗅探读取的头部字节上限；Tika 魔数识别所需远小于此值，取 8KB 以覆盖
     * 个别头部偏移较深的容器格式。
     */
    private static final int SNIFF_HEAD_LIMIT = 8192;

    private final GuardConfig config;
    private final ClassLoader probeClassLoader;

    /**
     * 惰性单次探测结果缓存；<b>实例级</b>（P2 retrofit，同 mail {@code DefaultAttachmentGuard}
     * 范式）+ {@code volatile} 保证跨线程可见，无需额外同步（竞态下重复探测同一个只读判定，
     * 代价可忽略，同 ADR-0021 范式）。实例级使测试可用 {@code FilteredClassLoader} 真实演练
     * 缺 tika 分支，无需静态 override 钩子与复位纪律。
     */
    private volatile Boolean tikaPresentCache;

    /** 便捷构造：探测 ClassLoader 回退本类加载器（直接 new 使用的场景）。 */
    public DefaultStorageGuard(GuardConfig config) {
        this(config, DefaultStorageGuard.class.getClassLoader());
    }

    /**
     * @param probeClassLoader Tika classpath 探测所用 ClassLoader；装配层传容器侧
     *                         ClassLoader（{@code ResourceLoader#getClassLoader()}），使
     *                         {@code FilteredClassLoader} 条件测试对"缺 tika-core"分支
     *                         真实生效（同 mail 裁定 E 范式）。
     */
    public DefaultStorageGuard(GuardConfig config, ClassLoader probeClassLoader) {
        this.config = config;
        this.probeClassLoader = probeClassLoader;
    }
```

(b) `isTikaPresent`（:147-161）替换为实例方法：

```java
    /**
     * 惰性、缓存的 Tika 可用性探测（{@code initialize=false} 不触发类初始化，
     * 仅验证类可解析），经构造注入的 {@link #probeClassLoader} 探测（P2 retrofit，
     * 同 mail {@code DefaultAttachmentGuard#isTikaPresent} 范式）。
     */
    private boolean isTikaPresent() {
        Boolean cached = tikaPresentCache;
        if (cached != null) {
            return cached;
        }
        boolean present;
        try {
            Class.forName(TIKA_PROBE_CLASS, false, probeClassLoader);
            present = true;
        } catch (ClassNotFoundException e) {
            present = false;
        }
        tikaPresentCache = present;
        return present;
    }
```

(c) **删除** `overrideTikaPresent`/`resetTikaPresentCache` 两个静态钩子及其 Javadoc（:163-178）。

(d) 类级 Javadoc 中"本类因此在真正调用 {@code MimeTyping} 之前先做一次惰性、缓存的 classpath 探测（同一范式）"一句改为"本类因此在真正调用 {@code MimeTyping} 之前先做一次惰性、缓存的<b>实例级</b> classpath 探测（P2 retrofit 为 mail {@code DefaultAttachmentGuard} 的构造注入 probeClassLoader 范式）"。

- [ ] **Step 4: 实现——装配层传容器 ClassLoader**

`ToolboxStorageAutoConfiguration.java` 的 `toolboxStorageGuard`（:49-55）替换为（import 区补 `org.springframework.core.io.ResourceLoader;`）：

```java
    /**
     * 探测 Tika 所用 ClassLoader 取容器侧（{@code ResourceLoader#getClassLoader()}），使
     * {@code FilteredClassLoader} 条件测试对"缺 tika-core"分支真实生效（P2 retrofit，
     * 同 mail {@code ToolboxMailAutoConfiguration#toolboxMailAttachmentGuard} 先例）。
     */
    @Bean
    @ConditionalOnMissingBean
    public StorageGuard toolboxStorageGuard(ToolboxStorageProperties properties, ResourceLoader resourceLoader) {
        ToolboxStorageProperties.Guard guard = properties.guard();
        long maxSize = guard.maxSize() == null ? 0L : guard.maxSize().toBytes();
        return new DefaultStorageGuard(new GuardConfig(maxSize, guard.blockedExtensions(), guard.verifyMime()),
                resourceLoader.getClassLoader());
    }
```

- [ ] **Step 5: 跑模块测试确认全绿**

```powershell
mvn -pl storage-enhancement test
```

预期：PASS（其余 12 处 `new DefaultStorageGuard(config)` 单参构造调用不受影响；`ToolboxStorageAutoConfigurationTest` 的 guard bean 用例经 ApplicationContextRunner 注入 ResourceLoader 正常装配）。

- [ ] **Step 6: 提交**

```powershell
git add storage-enhancement
git commit -m "storage-enhancement: tika 探测 retrofit 实例级 probeClassLoader，删静态钩子（P2，M2 终审建议）"
```

---

### Task 8: storage — i18n MessageSource 补齐

00-overview §4.1 约定（`docs/design/00-overview.md:144`）：每模块注册 `i18n/toolbox-{module}-messages*.properties` bundle 的 MessageSource 参与 facility `AggregatedMessageSource` 聚合。storage 目前完全缺失（全仓唯一样板是 compare 的 `toolboxCompareMessageSource`，`ToolboxCompareAutoConfiguration.java:65-76`）。本任务补基础设施与键前缀约定；存量硬编码中文错误消息迁移 bundle 属后续独立决策（见"范围裁定"）。

**Files:**
- Create: `storage-enhancement/src/main/resources/i18n/toolbox-storage-messages.properties`
- Create: `storage-enhancement/src/main/resources/i18n/toolbox-storage-messages_zh_CN.properties`
- Modify: `storage-enhancement/src/main/java/cn/code91/toolbox/storage/autoconfigure/ToolboxStorageAutoConfiguration.java`
- Modify: `storage-enhancement/src/test/java/cn/code91/toolbox/storage/autoconfigure/ToolboxStorageAutoConfigurationTest.java`

**Interfaces:**
- Consumes: 无。
- Produces: bean 名 `toolboxStorageMessageSource`（`ResourceBundleMessageSource`，`@ConditionalOnMissingBean(name=...)` 可覆盖），basename `i18n/toolbox-storage-messages`。

- [ ] **Step 1: 写失败测试**

`ToolboxStorageAutoConfigurationTest.java` 追加（该类 runner 字段为 `contextRunner`，:34）：

```java
    @Test
    void messageSourceBeanRegisteredPerOverviewConvention() {
        // 00-overview §4.1：模块注册自有 MessageSource 参与 facility AggregatedMessageSource 聚合（P2 补齐）。
        contextRunner.run(context -> assertThat(context).hasBean("toolboxStorageMessageSource"));
    }
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
mvn -pl storage-enhancement test -Dtest=ToolboxStorageAutoConfigurationTest
```

预期：新用例 FAIL（bean 不存在）。

- [ ] **Step 3: 实现——bundle 文件**

`toolbox-storage-messages.properties`：

```properties
# toolbox-storage-messages：storage-enhancement 模块自有 i18n 键的生产 bundle。
# 约定：本模块拥有的键统一 toolbox.storage. 前缀（高碰撞命名规避，同 compare I4 教训——
# facility AggregatedMessageSource（@Primary）会把此处的键聚合暴露给任何引入 storage 的应用）。
# 当前无模块自有键：错误消息暂为硬编码中文，迁移 bundle 属后续独立决策；本文件先落
# 00-overview §4.1 约定的 MessageSource 基础设施与键前缀纪律（P2）。
```

`toolbox-storage-messages_zh_CN.properties`：

```properties
# toolbox-storage-messages 简体中文 locale 变体；键前缀纪律见基础 bundle 头注释。当前无模块自有键。
```

- [ ] **Step 4: 实现——注册 MessageSource bean**

`ToolboxStorageAutoConfiguration.java` 顶层追加 `@Bean`（import 区补 `org.springframework.context.support.ResourceBundleMessageSource;`），放在 `toolboxStorageGuard` 之后：

```java
    /**
     * 模块 i18n（00-overview §4.1 约定补齐，P2）：注册模块自有 MessageSource 参与 facility
     * {@code AggregatedMessageSource}（{@code @Primary}）聚合。bundle 当前无模块自有键
     * （错误消息暂为硬编码中文，迁移属后续独立决策），先落基础设施与
     * {@code toolbox.storage.} 键前缀纪律（同 compare {@code toolboxCompareMessageSource} 样板）。
     */
    @Bean("toolboxStorageMessageSource")
    @ConditionalOnMissingBean(name = "toolboxStorageMessageSource")
    public ResourceBundleMessageSource toolboxStorageMessageSource() {
        ResourceBundleMessageSource messageSource = new ResourceBundleMessageSource();
        messageSource.setBasename("i18n/toolbox-storage-messages");
        messageSource.setDefaultEncoding("UTF-8");
        messageSource.setUseCodeAsDefaultMessage(false);
        messageSource.setFallbackToSystemLocale(false);
        return messageSource;
    }
```

- [ ] **Step 5: 跑模块测试确认全绿**

```powershell
mvn -pl storage-enhancement test
```

预期：PASS（含 `enabledFalseAssemblesNothing`——总开关关闭时本 bean 随整个装配类不注册，无需单独条件）。

- [ ] **Step 6: 提交**

```powershell
git add storage-enhancement
git commit -m "storage-enhancement: 补齐模块 i18n MessageSource 与键前缀纪律（P2，00-overview §4.1 约定）"
```

---

### Task 9: llm — Model.toString apiKey null 感知

`ToolboxLlmProperties.Model.toString`（97dfc23 引入）恒印 `apiKey=******`，即使 apiKey 为 null——与 mail `SmtpConnectionConfig.toString`（`password == null ? "null" : "******"`，`SmtpConnectionConfig.java:34`）的既有范式分歧（终审 cosmetic Minor）。只改 `Model`；`OpenAiModelConfig` 不改——其紧凑构造器对 null apiKey fail-fast（`IllegalStateException`），null 分支不可达。

**Files:**
- Modify: `llm-enhancement/src/main/java/cn/code91/toolbox/llm/autoconfigure/ToolboxLlmProperties.java:78-83`
- Modify: `llm-enhancement/src/test/java/cn/code91/toolbox/llm/autoconfigure/ToolboxLlmPropertiesTest.java`

**Interfaces:**
- Consumes: 无。
- Produces: `Model.toString()` 输出 `apiKey=null`（apiKey 为 null 时）或 `apiKey=******`（非 null）。Task 10 在此基础上追加 `jsonMode` 字段输出。

- [ ] **Step 1: 写失败测试**

`ToolboxLlmPropertiesTest.java` 追加：

```java
    @Test
    void modelToStringPrintsNullWhenApiKeyAbsent() {
        // 对齐 mail SmtpConnectionConfig 范式：null 印 null（诊断时可区分"未配置"与"已配置被遮蔽"），
        // 非 null 才遮蔽为 ******（cosmetic Minor，P2）。
        ToolboxLlmProperties.Model model = new ToolboxLlmProperties.Model(
                "openai-compatible", "https://api.deepseek.com/v1", null,
                "deepseek-chat", null, null, null, null, null);

        String rendered = model.toString();

        assertThat(rendered).contains("apiKey=null");
        assertThat(rendered).doesNotContain("******");
    }
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
mvn -pl llm-enhancement test -Dtest=ToolboxLlmPropertiesTest
```

预期：新用例 FAIL（当前恒印 `******`）。

- [ ] **Step 3: 实现**

`ToolboxLlmProperties.Model.toString`（:78-83）替换为：

```java
        /**
         * 覆盖 record 默认 {@code toString()}（全局约束 12：api-key 绝不出现在
         * toString/日志/错误消息；配置绑定诊断/调试打印若不慎调用本方法，防止明文外泄）。
         * null 时印 {@code null}（诊断时可区分"未配置"与"已配置被遮蔽"），非 null 才遮蔽——
         * 同 mail {@code SmtpConnectionConfig} 范式（P2 cosmetic 对齐）。
         */
        @Override
        public String toString() {
            return "Model[type=" + type + ", baseUrl=" + baseUrl
                    + ", apiKey=" + (apiKey == null ? "null" : "******") + ", model=" + model
                    + ", temperature=" + temperature + ", maxTokens=" + maxTokens + ", timeout=" + timeout
                    + ", maxRetries=" + maxRetries + ", rateLimitQps=" + rateLimitQps + "]";
        }
```

- [ ] **Step 4: 跑测试确认全绿 + 提交**

```powershell
mvn -pl llm-enhancement test -Dtest=ToolboxLlmPropertiesTest
```

预期：PASS（含既有 `modelToStringNeverExposesApiKey`——非 null 路径仍遮蔽）。

```powershell
git add llm-enhancement
git commit -m "llm-enhancement: Model.toString apiKey null 感知，对齐 mail 遮蔽范式（P2 cosmetic）"
```

---

### Task 10: llm — json-mode 接线 response_format（消费 JSON_OBJECT 死常量）

`OpenAiChatCompletionRequest.OpenAiResponseFormat.JSON_OBJECT`（`OpenAiChatCompletionRequest.java:26`）全仓零读取；`response_format` 序列化字段已就位（`:22`，`@JsonInclude(NON_NULL)` null 不下发）；`toProtocolRequest` 末参恒传 null（`OpenAiCompatibleClient.java:414`）。终审裁定"接线或删"——**选接线**：新增 per-model 开关 `toolbox.llm.models.<name>.json-mode`（默认 false；放 per-model 层——response_format 支持度因端点而异，正是裁定 B"并非所有兼容端点支持"的原因，不是模块级横切策略）。语义：**仅 `chatStructured` 且 json-mode 开启**时附带 `response_format={"type":"json_object"}`；`chat()` 永不附带；剥壳解析保持无条件鲁棒（裁定 B：可选附带，解析不依赖它）。

**Files:**
- Modify: `llm-enhancement/src/main/java/cn/code91/toolbox/llm/autoconfigure/ToolboxLlmProperties.java`（Model 追加组件）
- Modify: `llm-enhancement/src/main/java/cn/code91/toolbox/llm/openai/OpenAiModelConfig.java`（追加组件）
- Modify: `llm-enhancement/src/main/java/cn/code91/toolbox/llm/autoconfigure/ToolboxLlmAutoConfiguration.java:74-78`（透传）
- Modify: `llm-enhancement/src/main/java/cn/code91/toolbox/llm/openai/OpenAiCompatibleClient.java`（chat/chatStructured → doChat(jsonMode) → sendWithRetry → sendOnce → toProtocolRequest 全链穿参）
- Modify: `llm-enhancement/src/test/java/cn/code91/toolbox/llm/openai/OpenAiCompatibleClientStructuredOutputTest.java`（契约新用例 + clientOf 变体）
- Modify: 所有 `new ToolboxLlmProperties.Model(...)` 与 `new OpenAiModelConfig(...)` 测试构造点（record 组件追加导致签名变化）
- Modify: `docs/design/03-llm-enhancement.md` §6（配置样例补 json-mode 行）

**Interfaces:**
- Consumes: Task 9 后的 `Model.toString`（本任务在其输出末尾追加 `, jsonMode=<值>`）。
- Produces: `ToolboxLlmProperties.Model` 新组件 `Boolean jsonMode`（末位）；`OpenAiModelConfig` 新组件 `boolean jsonMode`（末位）；行为——json-mode=true 的模型上 `chatStructured` 请求体含 `"response_format":{"type":"json_object"}`。

- [ ] **Step 1: 写失败测试（WireMock 契约）**

`OpenAiCompatibleClientStructuredOutputTest.java`：

(a) import 区追加静态导入：

```java
import static com.github.tomakehurst.wiremock.client.WireMock.matching;
import static com.github.tomakehurst.wiremock.client.WireMock.notMatching;
import static com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor;
import static com.github.tomakehurst.wiremock.client.WireMock.verify;
```

(b) `clientOf()`（:43-47）改为委托带参变体（**注意**：`new OpenAiModelConfig(...)` 的末位新参 `jsonMode` 在 Step 3 落地前不编译——本步先写测试，RED 即含编译错，符合预期）：

```java
    private OpenAiCompatibleClient clientOf() {
        return clientOf(false);
    }

    private OpenAiCompatibleClient clientOf(boolean jsonMode) {
        OpenAiModelConfig config = new OpenAiModelConfig("deepseek", "http://localhost:" + wireMock.port() + "/v1",
                "sk-test", "deepseek-chat", 0.2, 512, Duration.ofSeconds(5), 0, Duration.ofMillis(10), 0, jsonMode);
        return new OpenAiCompatibleClient(config, List.of(), (key, permits, capacity, qps) -> null, duration -> { });
    }
```

(c) 追加三个契约用例：

```java
    @Test
    void jsonModeOnAttachesResponseFormatOnStructuredCalls() {
        stubModelReply("{\"category\":\"billing\",\"urgency\":\"high\"}");

        clientOf(true).chatStructured(ChatRequest.user("x"), TicketInfo.class);

        verify(postRequestedFor(urlEqualTo("/v1/chat/completions"))
                .withRequestBody(matching(".*\"response_format\"\\s*:\\s*\\{\\s*\"type\"\\s*:\\s*\"json_object\"\\s*\\}.*")));
    }

    @Test
    void jsonModeOffOmitsResponseFormat() {
        stubModelReply("{\"category\":\"billing\",\"urgency\":\"high\"}");

        clientOf(false).chatStructured(ChatRequest.user("x"), TicketInfo.class);

        verify(postRequestedFor(urlEqualTo("/v1/chat/completions"))
                .withRequestBody(notMatching(".*response_format.*")));
    }

    @Test
    void jsonModeOnDoesNotAffectPlainChat() {
        // json-mode 是 chatStructured 专属提示：普通 chat 即便模型开了 json-mode 也不附带
        // （否则普通对话被强制 JSON 输出）。
        stubModelReply("普通回答");

        clientOf(true).chat(ChatRequest.user("x"));

        verify(postRequestedFor(urlEqualTo("/v1/chat/completions"))
                .withRequestBody(notMatching(".*response_format.*")));
    }
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
mvn -pl llm-enhancement test -Dtest=OpenAiCompatibleClientStructuredOutputTest
```

预期：编译失败（`OpenAiModelConfig` 尚无 11 参构造）——RED 态。

- [ ] **Step 3: 实现——两个 record 追加组件**

(a) `OpenAiModelConfig.java` record 头（:24-27）追加末位组件并补 `@param`：

```java
public record OpenAiModelConfig(
        String modelName, String baseUrl, String apiKey, String model,
        Double temperature, Integer maxTokens, Duration timeout,
        int maxRetries, Duration retryBackoff, double rateLimitQps, boolean jsonMode) {
```

`@param` 区追加：

```java
 * @param jsonMode         {@code chatStructured} 时是否附带 {@code response_format={"type":"json_object"}}
 *                         （P2 接线；仅部分 OpenAI 兼容端点支持，故为 per-model 开关；剥壳解析
 *                         无论开关与否保持鲁棒——裁定 B"可选附带"）
```

toString（:63-69）末尾在 `rateLimitQps=` 之后追加 `+ ", jsonMode=" + jsonMode`：

```java
    @Override
    public String toString() {
        return "OpenAiModelConfig[modelName=" + modelName + ", baseUrl=" + baseUrl
                + ", apiKey=******, model=" + model + ", temperature=" + temperature
                + ", maxTokens=" + maxTokens + ", timeout=" + timeout + ", maxRetries=" + maxRetries
                + ", retryBackoff=" + retryBackoff + ", rateLimitQps=" + rateLimitQps
                + ", jsonMode=" + jsonMode + "]";
    }
```

(b) `ToolboxLlmProperties.Model` record 头（:63-66）追加末位组件 `Boolean jsonMode`（可空，绑定缺省即 null→按 false 处理）：

```java
    public record Model(
            String type, String baseUrl, String apiKey, String model,
            Double temperature, Integer maxTokens, Duration timeout,
            Integer maxRetries, Double rateLimitQps, Boolean jsonMode) {
```

`@param` 区追加：

```java
     * @param jsonMode      {@code chatStructured} 请求是否附带 {@code response_format} JSON mode
     *                      提示（默认 false；仅部分兼容端点支持，per-model 开关，P2 接线）
```

toString 末尾 `rateLimitQps=` 之后追加 `+ ", jsonMode=" + jsonMode`（保持 Task 9 的 apiKey 三元不动）。

- [ ] **Step 4: 实现——装配透传**

`ToolboxLlmAutoConfiguration.buildClient`（:74-78）的 config 构造替换为：

```java
        OpenAiModelConfig config = new OpenAiModelConfig(
                name, model.baseUrl(), model.apiKey(), model.model(),
                model.temperature(), model.maxTokens(), model.timeout(),
                model.maxRetries() == null ? -1 : model.maxRetries(),
                (Duration) null, model.rateLimitQps() == null ? 0 : model.rateLimitQps(),
                Boolean.TRUE.equals(model.jsonMode()));
```

- [ ] **Step 5: 实现——客户端全链穿参**

`OpenAiCompatibleClient.java`：

(a) `chat`（:122-137）与 `chatStructured`（:139-146）替换为：

```java
    @Override
    public Result<ChatResponse, LlmError> chat(ChatRequest request) {
        return doChat(request, false);
    }

    @Override
    public <T> Result<T, LlmError> chatStructured(ChatRequest request, Class<T> targetType) {
        Result<ChatResponse, LlmError> chatResult = doChat(request, config.jsonMode());
        if (chatResult.isErr()) {
            return Result.err(chatResult.getErr());
        }
        return parseStructured(chatResult.get().content(), targetType);
    }

    /**
     * chat/chatStructured 共用的编排主干。{@code jsonMode=true}（仅 chatStructured 且模型
     * 配置 {@code json-mode} 开启）时请求附带 {@code response_format={"type":"json_object"}}——
     * 裁定 B"可选附带"：并非所有兼容端点支持该字段，剥壳解析无论如何保持鲁棒，不依赖它。
     */
    private Result<ChatResponse, LlmError> doChat(ChatRequest request, boolean jsonMode) {
        LlmError rateLimited = checkRateLimit();
        if (rateLimited != null) {
            return Result.err(rateLimited);
        }

        logRequest(request);
        Instant start = Instant.now();
        Result<ChatResponse, LlmError> result = sendWithRetry(request, jsonMode);
        if (result.isOk()) {
            logResponse(result.get());
            notifyUsageListeners(result.get().usage(), Duration.between(start, Instant.now()));
        }
        return result;
    }
```

(b) `sendWithRetry`（:295）签名与 sendOnce 调用点改为 `private Result<ChatResponse, LlmError> sendWithRetry(ChatRequest request, boolean jsonMode)` / `sendOnce(request, jsonMode)`（方法体其余不动）。

(c) `sendOnce`（:347-348）签名与首行改为：

```java
    private Result<ChatResponse, LlmError> sendOnce(ChatRequest request, boolean jsonMode) {
        OpenAiChatCompletionRequest body = toProtocolRequest(request, jsonMode);
```

(d) `toProtocolRequest`（:407-415）替换为：

```java
    private OpenAiChatCompletionRequest toProtocolRequest(ChatRequest request, boolean jsonMode) {
        List<OpenAiChatMessage> messages = request.messages().stream()
                .map(m -> new OpenAiChatMessage(roleOf(m.role()), m.content()))
                .toList();
        ChatOptions options = request.options();
        Double temperature = options.temperature() != null ? options.temperature() : config.temperature();
        Integer maxTokens = options.maxTokens() != null ? options.maxTokens() : config.maxTokens();
        return new OpenAiChatCompletionRequest(config.model(), messages, temperature, maxTokens,
                jsonMode ? OpenAiChatCompletionRequest.OpenAiResponseFormat.JSON_OBJECT : null);
    }
```

- [ ] **Step 6: 修复全部测试构造点**

record 组件追加导致既有构造不编译。逐一追参（**不改任何断言语义**）：

```powershell
# 找出全部构造点
grep -rn "new OpenAiModelConfig(" llm-enhancement/src
grep -rn "new ToolboxLlmProperties.Model(" llm-enhancement/src
```

- `new OpenAiModelConfig(...)`：`OpenAiModelConfigTest`（7 处）、`OpenAiCompatibleClientTest.configOf/configWithRateLimit`、`OpenAiCompatibleClientMaskingTest`、`OpenAiCompatibleClientLoggingTest` 等测试助手——末位统一追加 `, false`（StructuredOutputTest 已在 Step 1 改为传 `jsonMode` 变量）。
- `new ToolboxLlmProperties.Model(...)`：`ToolboxLlmPropertiesTest` 4 处（:33、:41、:49 与 Task 9 新用例）及 `ToolboxLlmAutoConfigurationTest` 中如有——末位统一追加 `, null`。
- `OpenAiModelConfigTest.toStringNeverExposesApiKey` 断言无需改（仍 contains `******`）。

- [ ] **Step 7: 跑模块测试确认全绿**

```powershell
mvn -pl llm-enhancement test
```

预期：PASS（Step 1 三例转绿；`requestBodySerializesMessagesAndOptionsCorrectly` 等既有契约不受影响——默认 jsonMode=false 不下发 response_format）。

- [ ] **Step 8: 设计文档 §6 补配置行**

`docs/design/03-llm-enhancement.md` §6 的 YAML 配置样例（约 :166-181，模型条目 `rate-limit-qps` 行附近）在模型条目末尾追加一行：

```yaml
        json-mode: false      # chatStructured 附带 response_format JSON mode 提示（P2 接线；仅部分兼容端点支持）
```

（以该文件实际缩进为准；若 §6 是字段表格式，则加一行 `| json-mode | chatStructured 是否附带 response_format={"type":"json_object"}，默认 false（P2 接线） |`。）

- [ ] **Step 9: 提交**

```powershell
git add llm-enhancement docs/design/03-llm-enhancement.md
git commit -m "llm-enhancement: per-model json-mode 接线 response_format，消费 JSON_OBJECT 死常量（P2）"
```

---

### Task 11: llm — logging 测试 @Isolated

`OpenAiCompatibleClientLoggingTest` 两例（:116、:129）mutate facility `LogUtil` 的进程级 `static volatile` 全局脱敏开关（`setMaskingEnabled`）；`@BeforeEach/@AfterEach` 已成对捕获/复原（:55、:68），顺序执行无害——但若未来开启 JUnit 并行执行会互相踩踏。加 `@Isolated` 钉住（JUnit 5.12.2 自带，零新依赖；grep 确认全仓仅此一个测试类调用 `setMaskingEnabled`）。

**Files:**
- Modify: `llm-enhancement/src/test/java/cn/code91/toolbox/llm/openai/OpenAiCompatibleClientLoggingTest.java:35`

**Interfaces:** 无。

- [ ] **Step 1: 加注解**

import 区追加 `org.junit.jupiter.api.parallel.Isolated;`，类声明（:35）改为：

```java
@Isolated("mutate facility LogUtil 进程级全局脱敏开关（setMaskingEnabled）：当前 surefire 顺序执行下"
        + " @AfterEach 已复原、无害；若启用 JUnit 并行执行，本类必须独占运行避免全局态互相踩踏（P2 钉住）")
class OpenAiCompatibleClientLoggingTest {
```

- [ ] **Step 2: 跑测试 + 提交**

```powershell
mvn -pl llm-enhancement test -Dtest=OpenAiCompatibleClientLoggingTest
```

预期：PASS（顺序执行下注解无行为影响）。

```powershell
git add llm-enhancement
git commit -m "llm-enhancement: logging 测试标注 @Isolated，钉住 LogUtil 全局态的并行隔离前提（P2）"
```

---

### Task 12: llm — 设计文档裁定 A supersede 补注

`docs/design/03-llm-enhancement.md` §4.1 仍在 `LlmClient` 接口里列 `chatStream` 与 `StreamListener`、§3 表列 `chatAsync`——但 M4 实施计划裁定 A（`docs/plans/2026-07-07-m4-llm.md:26`）已把流式/异步剔除出 P1（P1 接口只含 `chat`/`chatStructured`，无死桩）。补"P1 已 supersede"注，并顺手修正 `docs/design/00-overview.md:241` 的失效交叉引用（流式设计实际在 03 §2.3，非 §4.4）。纯文档任务。

**Files:**
- Modify: `docs/design/03-llm-enhancement.md`（§4.1 :55 标题后、§3 :50 行）
- Modify: `docs/design/00-overview.md:241`

**Interfaces:** 无。

- [ ] **Step 1: §4.1 标题后加 supersede 注**

`docs/design/03-llm-enhancement.md` 的 `### 4.1 Seam 接口与值对象（`core`）` 标题（:55）之后、代码块之前插入：

```markdown
> **P1 实施注（2026-07-08 补记）**：下方接口所列 `chatStream` 与 `StreamListener` 已被 M4 实施
> 计划**裁定 A**（`docs/plans/2026-07-07-m4-llm.md`）从 P1 剔除——P1 落地的 `LlmClient` 仅含
> `chat` 与 `chatStructured`，代码中无任何流式/异步死桩。流式与 `chatAsync` 见 §10 P2 行。
```

- [ ] **Step 2: §3 表 chatAsync 行加注**

`docs/design/03-llm-enhancement.md:50` 的

```
| `Async<T>` / 虚拟线程 | `chatAsync` |
```

替换为：

```
| `Async<T>` / 虚拟线程 | `chatAsync`（P2——裁定 A 剔除出 P1，见 §4.1 实施注） |
```

- [ ] **Step 3: 00-overview 交叉引用修正**

`docs/design/00-overview.md:241` 行尾说明列的

```
流式 SSE 用 JDK `java.net.http.HttpClient`，见 03 文档 §4.4
```

替换为：

```
流式 SSE 用 JDK `java.net.http.HttpClient`（P2，P1 按裁定 A 未实现），见 03 文档 §2.3
```

- [ ] **Step 4: 提交**

```powershell
git add docs/design
git commit -m "docs: llm 设计文档补裁定 A supersede 注，修正 00-overview 流式交叉引用（P2 doc drift）"
```

---

### Task 13: 全仓合并门禁

**Files:** 无改动。

- [ ] **Step 1: 全仓 clean verify（含 jacoco/analyze 门禁与 PG/MinIO Docker IT，需 Docker 运行中）**

```powershell
mvn clean verify
```

预期：BUILD SUCCESS，测试总数 ≥ 608（P1 基线）+ 本计划新增约 20 例；jacoco 三阈值、dependency:analyze 零告警全过。

- [ ] **Step 2: 汇报**

门禁通过后汇报控制器。按项目惯例，合并回 master 走"终审 → FF 合并 → 删分支"流程，**不在本计划内自动执行**。

---

## 自审记录（写计划时已核）

- **覆盖核对**：P2 backlog 13 项 → T1(Date/UUID)、T2(带体枚举)、T3(include 深路径)、T4(nullAsEmpty 元素层)、T5(labelSupplier 加固)、T6(list Javadoc)、T7(tika retrofit)、T8(i18n MessageSource)、T9(toString cosmetic)、T10(JSON_OBJECT 死常量)、T11(@Isolated)、T12(doc drift)；storage 分片为 feature 级 roadmap 项，经范围裁定显式排除。
- **顺序依赖**：仅 T9→T10（同改两个 record 的 toString，T10 的追加以 T9 产物为基）。其余任务可独立执行，但建议按序（同文件改动少冲突）。
- **关键事实依据**：带体枚举 `E$1.isEnum()==false` 与 `isComparableTypePair` 先于叶子分派（`ReflectionDiffEngine.java:109-114`）；`java.sql.Date.toInstant()` 抛 UnsupportedOperationException 故 Date 渲染走 `Instant.ofEpochMilli`；停机竞态测试必须用英文 locale 才能区分"解析成功(Order Amount)"与"回退(订单金额)"；`OpenAiModelConfig` 对 null apiKey fail-fast 故 T9 只改 `Model`；`@JsonInclude(NON_NULL)` 保证 jsonMode=false 时 response_format 不下发。
